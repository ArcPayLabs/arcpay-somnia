import { MarketingPage } from "@/components/StaticMarketingPage";

export default function SolutionsPage() {
  return <MarketingPage eyebrow="Solutions" title="Agent businesses need governed execution." body="ArcPay Somnia packages onboarding, agent discovery, work escrow, policy controls, privacy intents, and audit trails into one operator surface." />;
}
