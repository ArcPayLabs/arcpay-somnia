import { MarketingPage } from "@/components/StaticMarketingPage";

export default function DocsPage() {
  return <MarketingPage eyebrow="Docs" title="Everything judges need to verify." body="Use the Proofs page for deployment addresses, local commands, Supabase persistence, Azure worker status, and the Somnia testnet demo sequence." />;
}
