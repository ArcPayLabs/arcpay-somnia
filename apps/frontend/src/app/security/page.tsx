import { MarketingPage } from "@/components/StaticMarketingPage";

export default function SecurityPage() {
  return <MarketingPage eyebrow="Security" title="Policy before signature." body="Spending limits, allowed agents, approval thresholds, time windows, circuit breakers, and emergency pause controls are built into the contract and UI flow." />;
}
