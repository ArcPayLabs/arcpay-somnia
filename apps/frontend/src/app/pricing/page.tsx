import { MarketingPage } from "@/components/StaticMarketingPage";

export default function PricingPage() {
  return <MarketingPage eyebrow="Pricing" title="Built for testnet evaluation." body="ArcPay Somnia is open for hackathon testing. Operators only need Somnia testnet STT and optional SOMUSD test tokens to run the full demo path." />;
}
